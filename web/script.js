const C=document.querySelector('#content'),T=document.querySelector('#title');async function get(u,o){let r=await fetch(u,o);return r.json()}async function dashboard(b){active(b);T.textContent='Dashboard';try{let d=await get('/api/dashboard');C.innerHTML=`<div class="cards"><div class="card"><small>Total Patients</small><strong>${d.patients}</strong></div><div class="card"><small>Doctors</small><strong>${d.doctors}</strong></div><div class="card"><small>Appointments</small><strong>${d.appointments}</strong></div><div class="card"><small>Bills</small><strong>${d.bills}</strong></div></div><div class="panel"><b>System Status</b><p>Java backend connected to MySQL through JDBC.</p></div>`}catch(e){C.innerHTML='<div class="panel">Database connection error. Check MySQL/password.</div>'}}async function patients(b){active(b);T.textContent='Patients';try{let a=await get('/api/patients');C.innerHTML=`<div class="panel"><div class="panelhead"><b>Patient Records</b><button class="btn" onclick="addForm()">+ Add Patient</button></div>${a.length?`<table><tr><th>ID</th><th>Name</th><th>Age</th><th>Gender</th><th>Phone</th><th>Address</th></tr>${a.map(x=>`<tr><td>${x.id}</td><td>${x.name}</td><td>${x.age}</td><td>${x.gender}</td><td>${x.phone}</td><td>${x.address}</td></tr>`).join('')}</table>`:'<div class="empty">No patients yet.</div>'}</div>`}catch(e){C.innerHTML='<div class="panel">Database connection error.</div>'}}function addForm(){C.innerHTML='<div class="panel"><div class="panelhead"><b>Add Patient</b></div><form class="form" onsubmit="save(event)"><label>Name<input id="n" required></label><label>Age<input id="a" type="number" required></label><label>Gender<select id="g"><option>Female</option><option>Male</option><option>Other</option></select></label><label>Phone<input id="p" required></label><label class="full">Address<input id="ad"></label><div class="full"><button class="btn">Save to MySQL</button></div></form></div>'}async function save(e){e.preventDefault();let q=new URLSearchParams({name:n.value,age:a.value,gender:g.value,phone:p.value,address:ad.value});let r=await fetch('/api/patients',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:q});if(r.ok){alert('Saved in MySQL');patients(document.querySelectorAll('.nav')[1])}else alert('Database error')}function active(b){document.querySelectorAll('.nav').forEach(x=>x.classList.remove('active'));b.classList.add('active')}dashboard(document.querySelector('.nav'));
